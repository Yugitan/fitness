# Fitness健身记录 — 功能文档

> 最后更新：2026-05-18
> 版本：v1.1.0

---

## 一、健身动作库

### 功能概述
内置26个常用健身动作，覆盖6大训练分类，支持浏览、筛选、搜索。

### 详细功能
| 功能 | 说明 |
|------|------|
| 动作浏览 | 卡片式展示所有动作，含名称、分类、简介 |
| 分类筛选 | 胸部/背部/肩部/手臂/腿部/核心 六大分类筛选 |
| 关键词搜索 | 实时模糊搜索动作名称 |
| 动作详情 | 每个动作含讲解、训练要点、注意事项 |
| 视频播放 | 支持 HTML5 视频播放器（MP4格式） |
| 快速添加 | 从动作库一键添加到当日训练列表 |

### 路由
- `GET /exercise` — 动作库列表页
- `GET /exercise/{id}` — 动作详情页
- `GET /exercise/api/list` — 动作列表 API

---

## 二、训练记录模块

### 功能概述
记录每日健身训练数据，支持新增、编辑、删除、复制、完成标记。

### 详细功能
| 功能 | 说明 |
|------|------|
| 新增训练记录 | 选择日期、时长、备注，添加训练动作明细 |
| 动作明细 | 每组记录：动作名称、组数、次数、负重(kg) |
| 编辑记录 | 修改日期、时长、备注、动作明细 |
| 删除记录 | 软删除（逻辑删除） |
| 复制记录 | 一键复制历史记录到新日期 |
| 标记完成 | 自动统计总组数、总次数 |
| 日期筛选 | 按日期范围筛选历史记录 |
| 用户关联 | 登录用户记录绑定账号，普通用户仅见自己记录，管理员可查看全部 |

### 路由
- `GET /training` — 训练记录页面
- `GET /training/api/list` — 查询列表（支持 startDate/endDate/userId）
- `POST /training/api/create` — 新增记录
- `PUT /training/api/{id}` — 更新记录
- `DELETE /training/api/{id}` — 删除记录
- `POST /training/api/{id}/copy` — 复制记录
- `PUT /training/api/{id}/complete` — 标记完成

---

## 三、训练计划模板

### 功能概述
预置训练计划模板，按分组管理，支持套用生成训练清单。

### 详细功能
| 功能 | 说明 |
|------|------|
| 计划浏览 | 分组展示：新手入门/增肌计划/减脂计划/力量突破，普通用户仅见自己的计划 |
| 计划创建 | 自定义计划名称、分组、难度、动作列表，自动绑定创建用户 |
| 计划编辑 | 修改计划内容和动作安排 |
| 计划删除 | 软删除 |
| 计划复制 | 一键复制现有计划 |
| 套用模板 | 一键生成当日训练清单 |
| 完成标记 | 每组训练单独标记完成状态 |

### 路由
- `GET /plan` — 训练计划页面
- `GET /plan/api/list` — 计划列表 API
- `POST /plan/api/create` — 创建计划
- `PUT /plan/api/{id}` — 更新计划
- `DELETE /plan/api/{id}` — 删除计划
- `POST /plan/api/{id}/copy` — 复制计划

---

## 四、用户账号体系

### 功能概述
完整的用户注册、登录、权限管理，支持普通用户和管理员角色。

### 详细功能
| 功能 | 说明 |
|------|------|
| 用户注册 | 用户名4-20位字母数字下划线，密码6-20位，BCrypt加密存储 |
| 用户登录 | 用户名+密码验证，Session保持登录态 |
| 退出登录 | 清除Session，清除游客数据 |
| 角色区分 | 普通用户(0)/管理员(1)/超级管理员(2) |
| 账号状态 | 正常/禁用，管理员可切换 |
| 登录状态 | 导航栏动态显示用户信息，未登录显示登录/注册按钮 |

### 路由
- `GET /login` — 登录页面
- `GET /register` — 注册页面
- `POST /user/api/register` — 注册 API
- `POST /user/api/login` — 登录 API
- `POST /user/api/logout` — 退出 API
- `GET /user/api/current` — 获取当前登录用户

### 默认管理员
- 用户名：`admin`
- 密码：`123456`

---

## 五、游客模式

### 功能概述
未登录用户以游客身份有限使用APP，连续使用7天后强制登录。

### 详细功能
| 功能 | 说明 |
|------|------|
| 每日规则弹窗 | 每日首次打开首页弹出游客规则说明 |
| 使用天数追踪 | 前端 localStorage 记录首次访问时间 |
| 7天限制 | 连续使用满7天后弹出强制锁定弹窗，只能去登录/注册 |
| 权限限制 | 游客仅可浏览动作库、训练计划，隐藏博主专区/数据统计 |
| 写操作拦截 | 游客 POST/PUT/DELETE 请求返回 401 |
| 数据清理 | 后端启动时自动清理超7天的游客数据（userId为NULL） |
| 锁定弹窗 | 7天到期后不可关闭的强制登录弹窗 |

### 技术实现
- 前端：`GuestMode` 工具（common.js），localStorage 持久化
- 后端：`GuestInterceptor` 拦截写操作，`GuestService` 定时清理

---

## 六、管理员后台

### 功能概述
统一后台管理界面，支持仪表盘、用户管理、动作库管理、计划管理、系统配置。

### 6.1 仪表盘
- 统计卡片：总用户数、动作总数、训练记录数、计划总数（实时 API）
- 快捷入口：用户管理、动作库管理、系统配置

### 6.2 用户管理
- 用户列表：ID、用户名、昵称、角色、状态、创建时间
- 启用/禁用：切换用户账号状态
- 删除用户：软删除
- **修改密码**：管理员直接重置用户密码（无需旧密码）
- **新增用户**：管理员手动创建用户（账号、密码、昵称、角色、状态）

### 6.3 动作库管理
- 新增动作：名称、分类、讲解、训练要点、注意事项、排序
- 编辑动作：修改动作全部字段
- 删除动作：软删除

### 6.4 计划管理
- 计划列表：ID、名称、分组、难度
- 下架计划：软删除

### 6.5 博主管理（预留）
- 博主列表、新增博主（UI 就绪，后端待开发）

### 6.6 博主计划管理（预留）
- 博主计划列表、新增计划（UI 就绪，后端待开发）

### 6.7 系统配置
- 配置项列表：key-value 形式
- 在线编辑：直接修改配置值并保存

### 后台路由
| 页面 | 地址 |
|------|------|
| 管理员登录 | `GET /admin/login/page` |
| 仪表盘 | `GET /admin` |
| 用户管理 | `GET /admin/user/list` |
| 动作库管理 | `GET /admin/exercise/list` |
| 计划管理 | `GET /admin/plan/list` |
| 博主管理 | `GET /admin/blogger/list` |
| 博主计划管理 | `GET /admin/blogger/plan` |
| 系统配置 | `GET /admin/config` |

### 后台 UI 规范
- 统一使用 `admin/common/fragments.html` Thymeleaf Fragment
- 配色：静雅健身馆主题（sage绿 + gold金 + 暖白底色）
- 布局：顶部导航栏 + 左侧220px侧边栏 + 主内容区
- 全局一致：导航栏欢迎信息、侧边栏图标、退出按钮

---

## 七、博主专区（预留）

- 博主列表页、博主主页、训练计划展示
- 数据库已建表（blogger、blogger_plan）
- 前端页面框架已就绪，后端待开发

---

## 八、数据统计（预留）

- 训练数据可视化（Chart.js 已集成）
- 统计页面框架已就绪，后端待开发

---

## 技术架构

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Spring Boot 2.5.15 | 嵌入式 Tomcat 或外置部署 |
| MVC | Spring MVC 5.3.x | @Controller + @RestController |
| ORM | MyBatis 3.5.9 | XML Mapper + 接口代理 |
| 数据库 | MySQL 8.0.33 | 逻辑删除（is_deleted） |
| 模板引擎 | Thymeleaf 3.0.15 | Fragment 复用 |
| 前端 | Bootstrap 5.3.0 + Chart.js 2.9.4 | 响应式布局 |
| 密码加密 | BCrypt | Spring Security Crypto |
| 构建 | Maven 3.9.11 | WAR 部署 |
| Java | OpenJDK 14 | |
| 部署 | Tomcat 9.0.118 | 上下文路径 /fitness |
