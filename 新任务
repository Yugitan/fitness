通用开发总要求

严格按照既定项目规范、页面布局、配色风格、组件样式统一开发，所有功能逻辑严谨无漏洞，前端界面交互流畅，后端接口稳定可用，前后端数据互通正常。

1. 账号体系功能开发

完整实现用户登录、用户注册全套流程，包含账号密码校验、表单正则验证、登录状态保存、注册信息入库、登录权限区分功能，登录成功正常跳转首页，注册成功自动跳转登录页。

2. 训练记录模块开发

搭建完整添加健身训练记录功能，支持录入训练项目、训练重量、组数次数、训练时长、训练日期等核心数据，记录成功实时同步展示在个人训练列表，支持数据正常存储与查看。

3. 游客模式规则实现

1. 用户未登录以游客模式进入APP，每日首次打开首页自动弹窗提醒相关规则

2. 弹窗明确提示：游客模式仅可临时使用，连续使用满7天后强制跳转登录页面

3. 前端计时记录游客使用时长，累计满7天直接锁定游客权限，无法继续浏览操作，必须完成登录/注册才可正常使用，后端+前端双重配置游客数据定时清理机制，满7天无条件自动清除全部游客留存记录，不可恢复

4. 游客模式仅开放基础浏览、记录录入权限，限制高级账号专属功能

4. 管理员后台UI统一规范

1. 全后台所有页面、菜单、弹窗、按钮、排版、色调、布局统一固定样式

2. 严禁点击后台不同菜单栏、功能选项时出现界面样式、布局、配色错乱变换

3. 所有后台页面严格以 /fitness/admin 路径下的原版UI界面为唯一标准复刻对齐

4. 保持侧边栏、顶部导航、内容区、表单组件、表格样式全局高度一致

5. 管理员后台新增功能

在管理员后台用户管理板块内，新增独立手动添加用户功能入口

• 支持管理员自主填写账号、密码、用户昵称、权限等级、状态等信息

• 提交后直接新增正式系统用户，数据同步入库，新增用户可正常登录使用APP

• 新增用户列表实时刷新，与原有用户管理列表样式、操作逻辑保持统一

附加约束

1. 所有新增功能不改动原有成熟业务逻辑与页面结构

2. 代码简洁规范，注释清晰，便于后续迭代修改

3. 适配移动端页面展示，交互逻辑贴合健身APP使用场景