# Fitness健身记录 — 启动教程

## 环境信息

| 软件 | 版本 | 本机路径 |
|------|------|----------|
| JDK | 14 | `D:\Develop\jdk14` |
| Maven | 3.9.11 | `D:\Develop\apache-maven-3.9.11` |
| MySQL | 8.0.33 | `root / 123456` |
| Tomcat | 9.0.118 | `D:\Develop\apache-tomcat-9.0.118` |

> 前端为 React 19 + Next.js 15 静态导出 SPA，已内置在 WAR 包中，无需额外安装 Node.js 即可运行。

---

## 第一步：初始化数据库

**1. 确保 MySQL 服务已启动**

**2. 打开命令行终端，登录 MySQL：**

```bash
mysql -u root -p123456 --default-character-set=utf8mb4
```

**3. 依次执行 SQL 脚本：**

```sql
-- 建表
source D:/test-codes/fitness/sql/schema.sql;

-- 插入测试数据（26个动作 + 管理员账号）
source D:/test-codes/fitness/sql/data.sql;

-- 验证
USE fitness;
SELECT COUNT(*) FROM exercise;   -- 应显示 26
SELECT * FROM user;              -- 应显示 admin

-- 退出
EXIT;
```

---

## 第二步：确认数据库连接配置

文件位置：`src/main/resources/application.yml`

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fitness?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
```

---

## 第三步：编译打包

```bash
cd D:/test-codes/fitness
D:\Develop\apache-maven-3.9.11\bin\mvn clean package -Dmaven.test.skip=true
```

成功后WAR包位于：`target/fitness.war`

---

## 第四步：部署到 Tomcat

```bash
# 复制 WAR 包到 Tomcat
copy D:\test-codes\fitness\target\fitness.war D:\Develop\apache-tomcat-9.0.118\webapps\
```

> 如果Tomcat之前已启动，先执行 `D:\Develop\apache-tomcat-9.0.118\bin\shutdown.bat` 停止后再部署。

---

## 第五步：启动 Tomcat

直接双击运行 `D:\Develop\apache-tomcat-9.0.118\bin\startup.bat`

会弹出一个命令行窗口，等待出现以下日志表示启动完成：

```
Server startup in [xxx] milliseconds
```

> 如果窗口闪退看不到日志，用文本编辑器打开 `startup.bat`，在末尾添加一行 `pause`，保存后重新双击。

---

## 第六步：访问应用

浏览器打开以下地址：

| 页面 | 地址 |
|------|------|
| **前台首页** | http://localhost:8080/fitness/ |
| 动作库 | http://localhost:8080/fitness/exercise/ |
| 训练记录 | http://localhost:8080/fitness/training/ |
| 训练计划 | http://localhost:8080/fitness/plan/ |
| 数据统计 | http://localhost:8080/fitness/statistics/ |
| 博主专区 | http://localhost:8080/fitness/blogger/ |
| 用户登录 | http://localhost:8080/fitness/login/ |
| 用户注册 | http://localhost:8080/fitness/register/ |
| **管理后台** | http://localhost:8080/fitness/admin |

**管理员账号：** `admin` / `123456`

---

## 停止 Tomcat

双击运行 `D:\Develop\apache-tomcat-9.0.118\bin\shutdown.bat`

---

## 快速验证清单

- [ ] 首页能正常打开，展示运动主题 UI、分类卡片、精选动作
- [ ] 动作库可按分类筛选、模糊搜索、查看详情（含视频播放器）
- [ ] 训练记录可新增（含动态动作明细行）、编辑、删除、复制、标记完成
- [ ] 训练计划可按分组筛选、创建、编辑、删除、一键套用模板
- [ ] 数据统计页面展示 Recharts 图表（月度趋势、动作频率、组数次数组图）
- [ ] 用户注册/登录功能正常
- [ ] 管理员后台可用 admin/123456 登录，管理用户、动作、计划、博主、系统配置

---

## 常见问题

### Tomcat窗口闪退

用文本编辑器打开 `startup.bat`，在文件末尾添加一行 `pause`，保存后重新双击。窗口会停住显示错误信息。

### 启动日志报 `Ambiguous mapping`

说明有Spring MVC路径冲突。检查控制器类中是否有重复的 `@GetMapping` 路径。

### 数据库连接失败

- 确认MySQL服务已启动
- 确认 `fitness` 数据库已创建并执行了SQL脚本
- 检查 `application.yml` 中的 `username` 和 `password`

### 端口被占用

```bash
# 查看谁占用了8080端口
netstat -ano | findstr 8080

# 强制结束进程（替换为实际PID）
taskkill /F /PID <PID>
```

### 前端页面样式不显示

前端为 Next.js 静态导出 SPA，所有资源文件已内置在 WAR 包中。
- 确认已清除浏览器缓存（Ctrl+F5 强制刷新）
- 确认 WAR 包完整部署到 Tomcat webapps 目录
- 确认 `src/main/resources/static/` 目录包含 `_next/` 文件夹

### 重新部署步骤

如果修改了代码需要重新部署：

```bash
# 1. 停止Tomcat（双击 shutdown.bat）
# 2. 重新编译
cd D:/test-codes/fitness
D:\Develop\apache-maven-3.9.11\bin\mvn clean package -Dmaven.test.skip=true

# 3. 删除旧部署
rmdir /s /q D:\Develop\apache-tomcat-9.0.118\webapps\fitness
del D:\Develop\apache-tomcat-9.0.118\webapps\fitness.war

# 4. 复制新WAR包
copy target\fitness.war D:\Develop\apache-tomcat-9.0.118\webapps\

# 5. 启动Tomcat（双击 startup.bat）
```

### 前端开发（如需修改前端代码）

前端源码位于 `frontend/` 目录，技术栈为 React 19 + Next.js 15。

```bash
cd D:/test-codes/fitness/frontend

# 安装依赖（首次）
npm install --legacy-peer-deps

# 启动开发服务器（端口 3000，API 自动代理到 8080）
npm run dev

# 构建并导出到 Spring Boot 静态资源
npm run export
```

开发时访问 `http://localhost:3000/`，API 请求会自动代理到 `http://localhost:8080/fitness/`。
