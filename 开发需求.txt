请基于以下固定版本开发健身记录Web应用，标准Maven项目，可在Cursor中开发、打包并部署到Tomcat 9.0.89运行。采用高可扩展架构，预留全部后期功能迭代的接口与数据表，后期无需重构代码即可无缝新增：用户账号体系、训练计划分享与导入、社交互动、数据统计可视化、多端数据同步、知名博主专区、博主训练计划带视频链接、完整管理员后台等功能。

【硬性固定版本（禁止AI自动升级任何依赖，已100%验证无冲突）】
Java 14
Maven 3.9.11
Tomcat 9.0.89（最后一个支持Java EE 8的稳定版，完美兼容Spring Boot 2.5.x）
打包方式：war包，外置Tomcat部署（禁用Spring Boot内置Tomcat）
技术栈：
- Spring Boot 2.5.15（精确版本，完美支持Java 14）
- Spring MVC 框架
- MyBatis 3.5.9（精确版本，Spring Boot官方默认集成）
- MySQL 8.0.33（精确版本）
- 前端：Thymeleaf 3.0.15 模板引擎 + Bootstrap 5.3.0 响应式框架 + 原生JavaScript（禁用jQuery，减少依赖体积）
项目包名：com.fitness
服务端口：8080

【基础必须实现功能（AI必须100%完成，不能遗漏任何一项）】
1. 健身动作库
- 固定六大分类：胸部、背部、肩部、手臂、腿部、核心
- 每个动作包含：动作名称、所属分类、动作讲解、训练要点、注意事项、本地MP4示范小视频
- 页面内置HTML5原生视频播放器，支持：播放/暂停、全屏、音量调节，手机端自动适配屏幕尺寸
- 支持功能：按分类筛选动作、关键词模糊搜索动作、点击动作快速添加到当日训练列表
- 内置至少20个常用健身动作的测试数据（包含完整文字说明），部署完成后即可直接使用

2. 个人每日训练记录
- 可记录项：训练日期、选择动作、组数、每组次数、负重重量(kg)、训练时长(分钟)、备注信息
- 支持操作：新增记录、编辑记录、删除记录、单条记录复制
- 展示方式：按训练日期倒序排列，支持按日期范围筛选历史训练记录
- 当日训练完成后可一键标记"今日训练完成"，系统自动统计当日总组数、总训练次数

3. 个人训练计划模板
- 支持操作：自定义创建训练计划、保存计划、编辑计划、删除计划、复制已有计划
- 核心功能：一键套用模板生成当日训练清单，每组训练完成后可单独标记完成状态
- 支持计划分组管理（例如："新手入门"、"增肌计划"、"减脂计划"等分类）

【架构扩展预留要求（AI必须严格按照以下设计预留，不能简化任何部分）】
一、用户账号体系预留（支持后期无缝切换账号模式）
1. 数据库预留用户表（user）：
   主键ID、用户名、密码(BCrypt加密存储)、昵称、头像、邮箱、手机号、个人简介、
   角色(0-普通用户/1-管理员/2-超级管理员)、账号状态(0-禁用/1-正常)、
   创建时间、更新时间、是否删除(逻辑删除标记)
2. 所有业务表（训练记录表、个人计划表、收藏表、点赞表、评论表等）必须预留用户ID字段，关联用户表
3. 当前游客模式下，用户数据存储在浏览器本地LocalStorage；后期用户登录后，支持一键将本地数据同步到云端数据库
4. 业务层预留接口：用户注册、用户登录、密码找回、个人信息修改、退出登录
5. 前端页面右上角预留"登录/注册"入口，个人中心页面预留占位

二、训练计划分享与导入预留
1. 个人计划表（plan）新增字段：
   是否公开(0-私有/1-公开)、分享唯一标识、浏览次数、收藏次数、点赞次数、
   分享时间、创建用户ID、是否删除
2. 新增计划分享表（plan_share）：主键ID、计划ID、分享用户ID、分享类型(0-公开分享/1-私密链接分享)、
   分享过期时间、创建时间
3. 新增计划导入记录表（plan_import）：主键ID、用户ID、原计划ID、导入时间、是否删除
4. 业务层预留接口：生成分享链接、取消分享、通过分享唯一标识解析计划、一键导入他人计划
5. 前端计划详情页预留"分享"按钮，支持复制分享链接；预留"导入计划"功能入口

三、社交互动功能预留
1. 预留收藏表（collect）：主键ID、用户ID、收藏对象类型(0-个人计划/1-博主计划/2-动作)、收藏对象ID、创建时间
2. 预留点赞表（like_record）：主键ID、用户ID、点赞对象类型(0-计划/1-动作/2-评论)、点赞对象ID、创建时间
3. 预留评论表（comment）：主键ID、用户ID、评论对象类型、评论对象ID、评论内容、父评论ID(用于回复评论)、
   创建时间、是否删除
4. 预留关注表（follow）：主键ID、用户ID、被关注用户ID、创建时间
5. 前端计划详情页和动作详情页预留点赞、收藏、评论区域占位

四、数据统计可视化预留
1. 训练记录表新增字段：消耗卡路里(预留字段)、训练难度
2. 业务层预留统计接口：
   - 个人训练统计：总训练天数、总组数、总次数、月度/年度训练趋势
   - 动作统计：个人动作使用频率、平均训练重量
   - 计划统计：计划使用次数、分享次数
   - 平台统计：总用户数、总训练次数、热门动作/计划排行榜（供管理员后台使用）
3. 前端预留"数据统计"导航入口，页面预留折线图、柱状图展示区域（已在pom.xml中预留Chart.js依赖）

五、知名博主专区 完整架构预留
1. 数据库新增预留表：
   - 博主表（blogger）：
     主键ID、博主昵称、头像、个人简介、所属平台(来源：B站/抖音/小红书等)、平台主页链接、
     博主分类(健体/力量举/CrossFit/瑜伽等)、粉丝数、是否推荐置顶、
     排序权重(数字越大排名越靠前)、创建时间、更新时间、是否删除
   - 博主训练计划表（blogger_plan）：
     主键ID、关联博主ID、计划标题、封面图、难度等级(1-新手/2-进阶/3-高级)、
     适用人群、训练部位、训练天数、
     视频链接(支持B站/抖音/优酷等外部视频链接)、视频类型(0-本地视频/1-外部链接视频)、
     计划简介、详细训练安排、浏览次数、收藏次数、点赞次数、
     上架状态(0-下架/1-上架)、排序权重、创建时间、更新时间、是否删除
2. 业务层预留接口：
   - 博主列表查询、博主详情查询、博主全部训练计划查询
   - 博主计划详情查询、视频播放接口
   - 博主计划一键导入到个人训练模板接口
3. 前端页面预留：
   - 首页"热门博主推荐"展示模块
   - 博主列表页（支持按分类、热度、最新发布筛选）
   - 博主个人主页（展示博主基本信息+全部训练计划）
   - 博主计划详情页（支持播放视频、一键导入计划）

六、管理员后台 完整基础架构预留
1. 数据库层面：
   - 用户表已预留角色字段和账号状态字段，支持多角色权限管理
   - 所有业务表已预留逻辑删除字段，支持后台软删除操作
   - 预留系统配置表（system_config）：主键ID、配置键、配置值、配置说明、创建时间、更新时间
     用于存储网站名称、网站logo、备案号等系统基本配置信息

2. 业务架构预留：
   - 预留登录拦截器（LoginInterceptor）：拦截所有后台请求，验证用户是否登录且拥有管理员权限
   - 预留全局权限验证注解（@RequiresAdmin）：标注在需要管理员权限的接口方法上
   - 预留后台管理接口基础路径：/admin/**
   - 预留以下核心后台管理接口：
     * 管理员登录、退出登录接口
     * 用户管理接口：用户列表查询、禁用/启用用户账号、删除用户
     * 动作库管理接口：动作增删改查、动作分类管理
     * 个人计划管理接口：查看所有用户计划、下架违规计划
     * 博主管理接口：博主增删改查、推荐/取消推荐博主
     * 博主计划管理接口：博主计划增删改查、上架/下架计划
     * 系统配置管理接口：修改网站基本配置信息
   - 预留统一的后台接口返回结果格式，与前台接口区分

3. 前端页面预留：
   - 前台页面底部预留"管理员登录"入口
   - 预留后台登录页面
   - 预留后台管理主页面（侧边栏导航+顶部导航+内容展示区域）
   - 预留以下后台管理页面占位：
     * 仪表盘（展示平台核心统计数据）
     * 用户管理页面
     * 动作库管理页面
     * 个人计划管理页面
     * 博主管理页面
     * 博主计划管理页面
     * 系统配置页面
   - 后台页面采用与前台一致的深色风格，响应式适配电脑端

4. 后期可直接实现的功能：
   - 管理员使用账号密码登录后台
   - 管理所有用户账号，禁用违规用户
   - 管理动作库，新增/修改/删除健身动作
   - 管理博主信息和博主训练计划，上下架内容
   - 查看平台整体统计数据
   - 修改网站基本配置信息

七、其他扩展功能预留
1. 动作库扩展：预留用户上传动作、动作审核功能（动作表新增上传用户ID、审核状态字段）
2. 训练提醒扩展：预留训练提醒表（reminder），支持设置每日训练时间提醒
3. 数据导入导出：预留训练记录、训练计划批量导出Excel、批量导入接口
4. 多端同步：预留数据同步接口，支持后期对接小程序、APP实现多端数据同步
5. 云存储：预留阿里云OSS/腾讯云COS配置项，后期可将视频、图片存储到云端
6. 消息通知：预留消息表（message），支持系统消息、评论回复通知等功能

【架构编码规范（AI必须严格遵守）】
1. 严格分层架构：控制器层（Controller）→ 服务层（Service，接口+实现类）→ 数据访问层（Mapper）→ 实体层（Entity）
2. 统一返回结果：所有接口返回统一的结果对象，包含状态码、消息、数据三个部分
3. 统一异常处理：全局异常处理器，自定义业务异常类
4. 统一工具类：封装日期工具类、字符串工具类、结果工具类、BCrypt加密工具类等公共工具类
5. 数据库设计：所有表必须包含主键ID、创建时间、更新时间、是否删除四个字段，全部使用逻辑删除
6. 代码注释：所有类、公共方法、复杂逻辑代码必须有清晰的中文注释
7. 前端规范：组件化开发，复用动作卡片、计划卡片、博主卡片等组件，保持样式统一
8. 安全规范：用户密码使用BCrypt加密存储，防止SQL注入攻击，XSS跨站脚本防护

【输出要求（AI必须完整输出，不能省略任何部分）】
1. 完整的pom.xml配置文件，严格适配上述所有版本，配置war打包方式、Tomcat依赖、Chart.js依赖，无冗余依赖
2. 标准Maven项目全套代码：
   - 所有Java类（控制器、服务接口、服务实现类、数据访问接口、实体类、配置类、工具类、拦截器、注解）
   - 所有配置文件（application.yml、mybatis-config.xml、mapper/*.xml映射文件）
   - 所有前端页面（templates/*.html，包含前台页面和后台占位页面）
   - 所有静态资源（static/css样式文件、static/js脚本文件）
3. 完整的MySQL建表语句，包含所有基础表和扩展表，每个字段都有清晰的中文注释
4. 内置测试数据SQL脚本：至少20个常用健身动作的测试数据、1个默认管理员账号（用户名admin，密码123456）
5. 自动生成完整的README.md中文文档，放在项目根目录下，必须包含以下内容：
   - 项目介绍：功能概述、技术栈说明
   - 环境要求：Java、Maven、Tomcat、MySQL的精确版本要求
   - 项目目录结构：清晰展示标准Maven项目的目录结构
   - 数据库初始化步骤：创建数据库、执行SQL脚本的详细说明
   - 本地开发启动步骤：在Cursor中编译、打包、部署到Tomcat的详细命令
   - 访问地址：前台访问地址、管理员后台访问地址、默认管理员账号密码
   - 功能说明：已实现功能列表、预留扩展功能列表
   - 后期扩展开发指南：按优先级排序的扩展功能开发步骤
   - 常见问题排查：版本冲突、项目启动失败、数据库连接失败等常见问题的解决方案
6. 详细操作说明：
   - Cursor编译打包命令：mvn clean package -Dmaven.test.skip=true
   - Tomcat 9.0.89部署步骤（war包放置位置、启动命令、端口配置）
   - 功能测试说明
7. 所有代码必须可直接编译运行，无语法错误、无依赖缺失